# Overlay that builds the image from this checkout instead of pulling it.
#
#   docker compose -f docker-compose.yml -f docker-compose.build.yml up -d --build

services:
  let-it-ride:
    build:
      context: .
      dockerfile: Dockerfile
    image: let-it-ride:local
