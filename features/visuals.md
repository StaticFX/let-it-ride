Generally a lot of the menus are only on the center of the screen (30%). We can make them a bit wider on desktop and allow for more content before wraping it.

# Home screen

Keep as it is

# Rules screen

When a page gets too long, make it instead a new page. On desktop we can have it in columns, on mobile in rows. 

# Hosting screen

Players: List players, host has a kick button to remove unwanted players.
Overview: See what deck is selected, what goal (best of x rounds or first to x points).

Host has a change config button.

Start game (with a 5 second full screen countdown then).
Leave button

# Config screen

See a list of prebuilt decks, with a button (built own deck).
When a deck is selected, we display below it all the cards (card and how often it is in the game). Clicking on a card makes it big and displayed so the user can inspect it.

Then, best of x rounds or first to x points (increate the max points limit to 1000, default is 200).
Add in a new option, time for turn (default 30s). When a the timer is gone, the user just goes out, when its an action card, a random player gets it.